files = {
  "startup",
  "tablePersistence",
  "userIo",
  "stockpile"
}

for _, file in ipairs(files) do
  local from = "/downloads/stockpile/" .. file
  local to = "/" .. file
  print(from .. "-->" .. to)

  fs.move(from, to)
end

fs.delete("/downloads")
shell.run("startup")